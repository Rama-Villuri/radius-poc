# pam_radius_auth.conf — drop this file at /etc/pam_radius_auth.conf on the Raspberry Pi.
# Format:  server[:port]   shared-secret   timeout(s)
#
# TODO: replace MAC_MINI_IP with your Mac Mini's LAN IP (e.g. 192.168.1.10)
# The shared secret must match the value in docker/freeradius/clients.conf.

MAC_MINI_IP:1812   radius-shared-secret   3
